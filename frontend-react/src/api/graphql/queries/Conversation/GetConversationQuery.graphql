query GetConversation($id: ID) {
  conversation(id: $id) {
    data {
      id
      attributes {
        users {
          data {
            id
            attributes {
              username
              email
              name
              lastname
              avatar_url
            }
          }
        }
      }
    }
  }
}
